apiVersion: apiregistration.k8s.io/v1
kind: APIService
metadata:
  name: v1alpha2.{{ .Values.config.groupName }}
  labels:
    app: {{ include "dns-webhook.name" . }}
    chart: {{ include "dns-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    cert-manager.io/inject-ca-from: "{{ .Release.Namespace }}/{{ include "dns-webhook.servingCertificate" . }}"
spec:
  group: {{ .Values.config.groupName }}
  groupPriorityMinimum: 1000
  versionPriority: 15
  service:
    name: {{ include "dns-webhook.fullname" . }}
    namespace: {{ .Release.Namespace }}
  version: v1alpha2